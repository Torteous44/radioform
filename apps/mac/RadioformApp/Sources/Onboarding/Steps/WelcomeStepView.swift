import SwiftUI

/// Welcome step (no copy inside the envelope, just a simple continue)
struct WelcomeStepView: View {
    let onContinue: () -> Void

    var body: some View {
        // Empty - button is now in main OnboardingView
        Color.clear
    }
}
