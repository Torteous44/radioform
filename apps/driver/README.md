# apps/driver/

AudioDriverKit driver implementation.

## Purpose

Contains the virtual audio device driver that appears in macOS Sound settings as a selectable output.

This is the lowest-level system component and is kept **intentionally minimal** to maximize stability and reduce macOS update risk.
